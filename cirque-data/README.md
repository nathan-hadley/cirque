# Cirque Data (ARCHIVED)

> **This directory is no longer the source of truth.** As of
> [ADR 0001](../docs/adr/0001-cloud-source-of-truth.md), app data lives in
> Cloudflare D1/R2 and is edited via the `cirque-api` `/admin` page. This
> directory is kept for history and for the one-off import scripts under
> `cirque-api/scripts/`.


## JOSM

### Recommened Plugins

#### FastDraw
Install the FastDraw plugin to quickly create polygons (boulder outlines)
- Go to settings -> plugins
- Search for FastDraw
- Click on FastDraw and click on "Ok"

### Shortcuts
Zoom: Two fingers on trackpad or `+`/`-`
Move: `ctrl` + trackpad
Scale polygon: `cmd` + `option` + `click`
Rotate polygon: `cmd` + `shift` + `click`

https://josm.openstreetmap.de/wiki/Help/Action/Select
